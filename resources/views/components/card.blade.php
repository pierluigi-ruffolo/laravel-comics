<div class="comic-card">
    <div class="img-container">
        <img src="{{ $img }}" alt="{{ $title }}">
    </div>
    <div class="card-body">
        <h5 class="comic-title">{{ $title }}</h5>
    </div>
</div>